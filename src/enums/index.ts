export enum SensorTypes {
  ENERGY = "energy",
  VIBRATION = "vibration",
}

export enum SensorStatus {
  ALERT = "alert",
  OPERATING = "operating",
}

export enum Types {
  LOCATION = "location",
  ASSET = "asset",
  COMPONENT = "component",
}
